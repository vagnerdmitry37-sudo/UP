name: CI

on:
  push:
    branches:
      - master
      - develop

  pull_request:
    branches:
      - master
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore UP.sln

      - name: Build
        run: dotnet build UP.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test UP.IntegrationTests/UP.IntegrationTests.csproj --no-build --configuration Release