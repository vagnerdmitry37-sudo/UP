name: CI

on:
  push:
    branches:
      - master
      - develop

  pull_request:
    branches:
      - master
      - develop

jobs:
  # Integration-tests:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: '10.0.x'

  #     - name: Restore UP.Api
  #       run: dotnet restore UP.Api/UP.Api.csproj

  #     - name: Check API formatting
  #       run: dotnet format UP.Api/UP.Api.csproj --verify-no-changes --no-restore --exclude UP.Api/Migrations

  #     - name: Check integration tests formatting
  #       run: dotnet format UP.IntegrationTests/UP.IntegrationTests.csproj --verify-no-changes --no-restore

  #     - name: Run tests
  #       run: dotnet test UP.IntegrationTests/UP.IntegrationTests.csproj
  
  # Build:
  #   runs-on: ubuntu-latest
  #   environment: Dev

  #   # needs: Integration-tests

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Build Docker image
  #       run: |
  #         docker build \
  #           -f UP.Api/Dockerfile \
  #           -t ${{ secrets.DOCKERHUB_USERNAME }}/up-api:${{ github.sha }} \
  #           -t ${{ secrets.DOCKERHUB_USERNAME }}/up-api:latest \
  #           .

  #     - name: Push Docker image
  #       run: |
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/up-api:${{ github.sha }}
  #         docker push ${{ secrets.DOCKERHUB_USERNAME }}/up-api:latest

  Deploy:
    runs-on: ubuntu-latest
    environment: Dev
    # needs: Build

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
            role-to-assume: arn:aws:iam::558613451057:role/up-api-dev-github-role
            role-session-name: github-actions-${{ github.run_id }}
            aws-region: eu-central-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        working-directory: Infrastructure/Terraform/Environments/Dev
        run: terraform init

      - name: Terraform Plan
        working-directory: Infrastructure/Terraform/Environments/Dev
        run: terraform plan

      - name: Terraform Apply
        working-directory: Infrastructure/Terraform/Environments/Dev
        run: terraform apply -auto-approve